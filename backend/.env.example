# Copy to .env and fill in. Loaded by app.main via python-dotenv (override=False).
# The backend uses the OpenAI provider (gpt-5.5) by default.
OPENAI_API_KEY=sk-...
# Optional — only needed if you switch agent_factory back to the Anthropic provider.
ANTHROPIC_API_KEY=sk-ant-...
